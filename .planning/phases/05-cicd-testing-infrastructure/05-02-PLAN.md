---
phase: 05-cicd-testing-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/tests/CMakeLists.txt
  - .github/workflows/ci.yml
  - flutter/integration_test/smoke_test.dart
  - flutter/test/streaming_test.dart
autonomous: true
requirements: [CICD-04, TEST-01, TEST-02]

must_haves:
  truths:
    - "All C++ test files in core/tests/ are registered with ctest and run in CI on both Ubuntu and macOS"
    - "Integration test job on main branch runs a real Flutter integration test, not an echo placeholder"
    - "Dart tests cover FFI-layer streaming and model lifecycle with mocked native calls"
  artifacts:
    - path: "core/tests/CMakeLists.txt"
      provides: "ctest registration for all C++ test files including test_backend_detection"
      contains: "test_backend_detection"
    - path: "flutter/integration_test/smoke_test.dart"
      provides: "Minimal Flutter integration test for CI smoke testing"
      contains: "testWidgets"
    - path: "flutter/test/streaming_test.dart"
      provides: "Unit tests for streaming token generation with mocked FFI"
      contains: "TokenChunk"
    - path: ".github/workflows/ci.yml"
      provides: "Integration test job with real flutter test command"
  key_links:
    - from: "core/tests/CMakeLists.txt"
      to: "core/tests/test_backend_detection.cpp"
      via: "add_executable + add_test"
      pattern: "test_backend_detection"
    - from: ".github/workflows/ci.yml"
      to: "flutter/integration_test/smoke_test.dart"
      via: "flutter test integration_test"
      pattern: "integration_test"
---

<objective>
Register all C++ tests with ctest, create a minimal Flutter integration test, and add a streaming unit test to improve coverage.

Purpose: CI runs all existing tests (not just some), integration tests actually execute, and test coverage expands to untested areas.
Output: Updated CMakeLists.txt, new integration_test/smoke_test.dart, new streaming_test.dart, updated ci.yml integration job
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@core/tests/CMakeLists.txt
@core/tests/test_backend_detection.cpp
@.github/workflows/ci.yml
@flutter/test/edge_veda_test.dart
@flutter/test/android_plugin_test.dart
@flutter/pubspec.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register test_backend_detection in ctest and verify C++ test completeness</name>
  <files>core/tests/CMakeLists.txt</files>
  <action>
  Read `core/tests/test_backend_detection.cpp` to understand its dependencies and whether it needs a model file.

  In `core/tests/CMakeLists.txt`, add `test_backend_detection` as a test target:
  - `add_executable(test_backend_detection test_backend_detection.cpp)`
  - `target_link_libraries(test_backend_detection PRIVATE edge_veda_static)`
  - `target_include_directories(test_backend_detection PRIVATE ${CMAKE_SOURCE_DIR}/include)`
  - `set_target_properties(test_backend_detection PROPERTIES RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/tests)`
  - `add_test(NAME backend_detection COMMAND test_backend_detection)`

  If test_backend_detection.cpp requires a model file (check for TEST_MODEL_PATH or similar), gate it behind the `EDGE_VEDA_TEST_MODEL_PATH` conditional like test_model_backed. If it does NOT require a model, add it unconditionally like test_api_guards.

  Also add `target_compile_definitions(test_backend_detection PRIVATE EDGE_VEDA_TEST_HOOKS)` if the test uses test hooks (check for `#ifdef EDGE_VEDA_TEST_HOOKS` in the source).

  Do NOT modify existing test registrations. Only ADD the missing one.
  </action>
  <verify>
  Run: `grep -c 'add_test' core/tests/CMakeLists.txt` should return at least 3 (api_guards, model_backed conditionally, backend_detection).
  Run: `grep 'test_backend_detection' core/tests/CMakeLists.txt` should show the new registration.
  </verify>
  <done>All 4 C++ test files are accounted for in CMakeLists.txt: test_inference (build-only, no ctest), test_api_guards (ctest), test_model_backed (ctest conditional), test_backend_detection (ctest, newly added).</done>
</task>

<task type="auto">
  <name>Task 2: Create Flutter integration smoke test and wire CI job</name>
  <files>flutter/integration_test/smoke_test.dart, .github/workflows/ci.yml</files>
  <action>
  1. Create `flutter/integration_test/smoke_test.dart`:
     - Import `package:flutter_test/flutter_test.dart` and `package:integration_test/integration_test.dart`.
     - Call `IntegrationTestWidgetsFlutterBinding.ensureInitialized()`.
     - Write a single `testWidgets('app launches without crashing', ...)` test that:
       a. Pumps the example app's main widget (import from `package:edge_veda_example/main.dart` if available, or a minimal MaterialApp).
       b. Expects no exceptions during pump.
       c. Verifies a basic widget is present (e.g., `find.byType(MaterialApp)`).
     - This is intentionally minimal — a smoke test proving the app can launch. Real functional integration tests are device-dependent.
     - Add a comment: `// Smoke test: verifies app launches without crash. Device-specific tests require physical hardware.`

  NOTE: Check if `flutter/example/lib/main.dart` exists. If it does, the integration test should import and pump its main widget. If it does not, create a minimal test that pumps a simple MaterialApp. The integration_test directory should be INSIDE `flutter/` (not `flutter/example/`) since this tests the plugin itself.

  Actually, Flutter integration tests conventionally live in the example app. Check the standard Flutter plugin test structure. If `flutter/example/integration_test/` is the correct location, use that instead. The CI job should `cd flutter/example` and run `flutter test integration_test`.

  2. In `.github/workflows/ci.yml`, update the `integration-tests` job:
     - Replace the `flutter test integration_test || echo "Integration tests not configured yet"` line with a real command.
     - Since integration tests on CI (macOS runner, no physical device) are limited, use `flutter test integration_test` which runs on the test framework without a real device. This validates the test compiles and basic widget logic works.
     - Remove the `|| echo "Integration tests not configured yet"` fallback.
     - The job already has the right triggers (main branch only), dependencies (core-build, flutter-analyze), and Flutter setup.
     - Update the working-directory to match wherever the integration test file actually lives.

  3. Ensure `flutter/pubspec.yaml` (or `flutter/example/pubspec.yaml`) has `integration_test` in dev_dependencies. Check first — if it is missing, note it needs to be added. The `integration_test` package is from the Flutter SDK:
     ```yaml
     dev_dependencies:
       integration_test:
         sdk: flutter
     ```
  </action>
  <verify>
  Run: `test -f flutter/example/integration_test/smoke_test.dart && echo "integration test exists" || test -f flutter/integration_test/smoke_test.dart && echo "integration test exists"`
  Run: `grep -v 'echo.*not configured' .github/workflows/ci.yml | grep -c 'integration_test'` should return at least 1 (real test command).
  Run: `grep 'integration_test' flutter/example/pubspec.yaml 2>/dev/null || grep 'integration_test' flutter/pubspec.yaml 2>/dev/null` confirms the dependency exists.
  </verify>
  <done>A minimal integration smoke test exists and the CI integration-tests job runs it on main branch merges instead of echoing a placeholder.</done>
</task>

<task type="auto">
  <name>Task 3: Add streaming token generation unit test</name>
  <files>flutter/test/streaming_test.dart</files>
  <action>
  Create `flutter/test/streaming_test.dart` to test streaming behavior with mocked FFI calls. This addresses TEST-01 (mocked FFI tests for streaming).

  The test should:
  1. Import the necessary EdgeVeda classes (TokenChunk, GenerateOptions, etc.)
  2. Test `TokenChunk` creation and final chunk detection more thoroughly than the existing edge_veda_test.dart:
     - Test a sequence of TokenChunks simulating a stream (index 0..N, last has isFinal=true)
     - Test that tokens can be concatenated to form a complete response
     - Test empty token handling
  3. Test `GenerateOptions` with `stream: true`:
     - Verify `stream` property is correctly set
     - Test `copyWith` preserving stream setting
     - Test `toJson` includes stream flag
  4. Test edge cases in GenerateResponse:
     - Zero latency (division edge case in tokensPerSecond)
     - Very large token counts
     - Empty text response

  Pattern: Follow the same test structure as existing tests in flutter/test/ (imports, group/test structure, expect assertions). Read an existing test file first to match style exactly.

  Do NOT attempt to test actual FFI calls or native library loading — those require a running native library. Focus on the Dart-side data models and their behavior.
  </action>
  <verify>
  Run: `cd flutter && flutter test test/streaming_test.dart` should pass with 0 failures.
  Run: `cd flutter && flutter test` should pass all tests (existing + new).
  </verify>
  <done>streaming_test.dart exists with tests for token stream simulation, stream-mode GenerateOptions, and GenerateResponse edge cases. All tests pass.</done>
</task>

</tasks>

<verification>
1. `grep 'test_backend_detection' core/tests/CMakeLists.txt` shows registration
2. Integration test file exists and contains `testWidgets`
3. ci.yml integration-tests job has real test command (no "not configured" echo)
4. `cd flutter && flutter test` passes all tests including new streaming_test.dart
</verification>

<success_criteria>
- ctest discovers and can run test_backend_detection alongside existing tests
- CI integration-tests job on main branch runs a real smoke test
- Flutter test suite has a new streaming_test.dart that passes
- `flutter test` runs all 12 test files with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/05-cicd-testing-infrastructure/05-02-SUMMARY.md`
</output>
